// src/helpers/parsing-html.js


export const parsingStringToHtml = function(value) {
    return (
        Date.getNow('yyyy-MM-dd HH:mm:ss.SSS')
    );
};
export const parsingShowRandomString = (num) => {
    return (
        String.generateRandom(num)
    );
};