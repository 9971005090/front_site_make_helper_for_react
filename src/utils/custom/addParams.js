// src/utils/custom/addParams.js
const addParams = (params = null, form = null) => { // type, event/base/both

    // 아래 영역에 코드 작성
    ///////////////////////////////////////////////////////////////////////////////////////////////////////////
    ///////////////////////////////////////////////////////////////////////////////////////////////////////////

    return params;
};

export default addParams;
